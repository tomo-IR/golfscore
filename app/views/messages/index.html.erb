<h1>コースごとの掲示板を覗けます</h1>

<h2>全てのメッセージ</h2>
<div>
<% @messages.each do |m| %>
  <span><%= m.content %> </span>

  <span>@<%= m.user.name %> </span>

  <% end %>
  </div>

  <h2>コース毎のメッセージ</h2>
<div>
<% @every_course.each do |c| %>
  <span><%= link_to c, every_course_message_path(course: c) %> </span><br>
  <% end %>
</div>

<%= link_to "トップにもどる", root_path %>


